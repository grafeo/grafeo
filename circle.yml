dependencies:
  pre:
    - sudo pip install cpp-coveralls
test:
  pre:
    - mkdir build
    - cd build
    - cmake .. -DBUILD_DOCS=OFF -DBUILD_TESTING=ON -DCOVERALLS=ON -DCMAKE_BUILD_TYPE=Debug
    - make
    - make coveralls
